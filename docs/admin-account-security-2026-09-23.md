# 后台管理员账号安全收口（2026-09-23）

- 线上曾存在仍可使用初始凭据登录的默认管理员账号。已通过现有权限管理服务将该账号停用，并确认初始凭据登录返回 401；另一名有 `admin:permission` 的启用管理员保留，权限管理入口未中断。
- 操作前只备份了服务器上的 `admin_users` 表至 `/root/admin-users-before-default-disable-20260923.sql`（仅 root 可读），未复制凭据到本机。权限变更由服务记录审计。
- 本地 API 源码已移除默认/环境变量管理员回退；数据库查询失败不再放行。无角色管理员不再自动获得所有权限。seed 必须显式提供 `ADMIN_USERNAME` 和至少 16 位的 `ADMIN_PASSWORD`，且不会重新启用已停用账号。
- 本地 API 类型检查、38 个单元测试通过；独立 `yinshua_ci` 数据库中，缺少管理员凭据的 seed 按预期失败，提供测试凭据的 seed 成功，9 个集成测试通过。隔离测试数据库已关闭。**这些源码变更尚未发布到线上。**
- 线上 API 容器启动流程可能运行 `prisma:push` 和 seed，不得为发布或验证而直接重启。发布前需去除对生产库的自动播种/推表、备份数据库，部署后验证默认账号仍不可登录、保留的管理员可正常管理权限。
